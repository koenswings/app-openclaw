# Copy this file to .env and fill in your values
# (The engine will populate port automatically on first dock)

port=18789
hostname=openclaw-dev      # Tailscale device hostname (visible in your Tailnet)

# NOTE: TS_AUTHKEY is no longer set here — store it in secrets/tailscale_authkey.txt
# See secrets/README.md for instructions.
